%section#latestPosts
    %section
        %h2 Die letzten 3 Posts
        .section-body
            - @last_posts.each do |post|
                = render partial: '/posts/post_card', locals: {post: post, short: true}
                
